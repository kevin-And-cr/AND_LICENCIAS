# ─── SQL Server (ya existente, solo referencia) ────────────────────────────────
DB_HOST=host.docker.internal   # En Windows/Mac con Docker Desktop
# DB_HOST=172.17.0.1           # En Linux (IP de la red bridge de Docker)
DB_PORT=1433
DB_NAME=mi_base_de_datos
DB_USER=sa
DB_PASSWORD=
